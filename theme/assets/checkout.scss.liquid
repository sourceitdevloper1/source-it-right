@mixin transform($transforms) {
  -moz-transform: $transforms;
  -o-transform: $transforms;
  -ms-transform: $transforms;
  -webkit-transform: $transforms;
  transform: $transforms;
}

@mixin transition($transition) {
  -moz-transition: $transition;
  -o-transition: $transition;
  -ms-transition: $transition;
  -webkit-transition: $transition;
  transition: $transition;
}

.main{
  .main__header{
    .breadcrumb {
      .breadcrumb__item{
        .breadcrumb__link{
          color: #a287be;
        }
      }
    }
  }
  .step__footer__previous-link{
    color: #a287be;
    .icon-svg--color-accent{
      color: #a287be;
    }
  }
}

.field__input:focus,
.field__input-wrapper--flag-focus .field__input {
  outline: none;
  border-color: #a287be;
  -webkit-box-shadow: 0 0 0 1px #a287be;
  box-shadow: 0 0 0 1px #a287be;
}

.btn,
.sp-modal-toggle{
  background-color: #a287be;
  color: #fff;
  &:hover {
    background-color: rgba(162, 135, 190, 0.7);
    color: white;
  }
}

.input-checkbox:focus,
.input-radio:focus {
  border: 2px solid #a287be;
  outline: none;
}

.input-checkbox:checked,
.input-radio:checked {
  border: none;
  -webkit-box-shadow: 0 0 0 10px #a287be inset;
  box-shadow: 0 0 0 10px #a287be inset;
}

a,
.link{
  color: #a287be;
  &:hover{
    color: #a287be;
    text-decoration: underline;
  }
}

.main .icon-svg--color-adaptive-light {
  color: #a287be;
  fill: currentColor;
}


.sidebar{
  background: #fbf9f6;
}

.new_pincode-section{
  #msg_error{
    &.error-msg{
      display: block;
      width: 100%;
      padding: 15px 10px;
    }
  }
}

.page--thank-you{
  .icon-svg--color-accent{
    color: #a287be;
    fill: currentColor;
    stroke: #a287be;
  }
}

.gst-data{
  .custom--filled{
    .information-icon-wrap{
      position: relative;
      .input-wrap {
        width: 90%;
        .field__label {
          opacity: 1;
        }
      }
      .info-icon{
        position: absolute;
        right:10px;
        @include transform(translateY(-50%));
        top:50%;
        background: #a287be;
        padding: 10px;
        border-radius: 50%;
        width: 5px;
        height: 5px;
        text-align: center;
        line-height: 5px;
        color: #fff;
        font-size: 14px;
        font-weight: bold;
        cursor: poiner;
        @media(max-width: 1024px){
          right: 0;
        }
      }
      .info-text{
        display: none;
        position: absolute;
        width: 85%;
        text-align: center;
        @include transform(translateY(-50%));
        top: 50%;
        font-size: 12px;
        right: 45px;
        line-height: 25px;
        background: #a287be;
        z-index: 99;
        color: #fff;
        @include transition(all 1s);
        &.active{
          display: block;
        }
        &:after{
          @include transform(translateY(-50%) rotate(25deg));
          content: '▼';
          position: absolute;
          right: -7px;
          color: #a287be;
          top: 50%;
        }
      }
    } 
  }
}
/* checkout phone prefix css */
.floating-labels .phone_label.field__label{
   width: 85%;
   display: inline-block;
   position: absolute;
   right: 0;
   @media(max-width: 767px){
    width: 86%;
   }
  }
  #checkout_shipping_address_phone_clone{
    width: 100%;
    display: inline-block;
    float: right;
    border-color: #d9d9d9;
    padding-left: 17%;
  }
  .country_code{
    float: left;
    text-align: center;
    background-color: #ddd;
    line-height: 3.3;
    width: 15%;
    position: absolute;
    border-radius: 4px 0 0px 4px;
  }
  .section--shipping-address #phone_tooltip{
    top:24px;
  }
  /* end checkout phone prefix css*/

.order-summary-toggle__icon,
.order-summary-toggle__dropdown,
.order-summary-toggle:hover .order-summary-toggle__icon,
.order-summary-toggle:focus .order-summary-toggle__icon,
.order-summary-toggle:hover .order-summary-toggle__dropdown,
.order-summary-toggle:focus .order-summary-toggle__dropdown{
  fill: #a287be;
}
.order-summary-toggle__text,
.order-summary-toggle:hover .order-summary-toggle__text,
.order-summary-toggle:focus .order-summary-toggle__text
{
  color: #a287be;
}
.section.section--billing-address{
  .section__header{
    .section__text{
      display: none;
    }
  }
}